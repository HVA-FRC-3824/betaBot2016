// RobotBuilder Version: 2.0
//
// This file was generated by RobotBuilder. It contains sections of
// code that are automatically generated and assigned by robotbuilder.
// These sections will be updated in the future when you export to
// Java from RobotBuilder. Do not put any code or make any change in
// the blocks indicating autogenerated code or it will be lost on an
// update. Deleting the comments indicating the section will prevent
// it from being updated in the future.

package org.usfirst.frc3824.BetaBot.commands;

import edu.wpi.first.wpilibj.command.CommandGroup;

import org.usfirst.frc3824.BetaBot.Constants;
import org.usfirst.frc3824.BetaBot.Robot;

/**
 *
 */
public class AutonomousLowBar extends CommandGroup
{
	// BEGIN AUTOGENERATED CODE, SOURCE=ROBOTBUILDER ID=PARAMETERS
    public AutonomousLowBar(int ShotChoice) {

    // END AUTOGENERATED CODE, SOURCE=ROBOTBUILDER ID=PARAMETERS

		// BEGIN AUTOGENERATED CODE, SOURCE=ROBOTBUILDER ID=COMMAND_DECLARATIONS

    // END AUTOGENERATED CODE, SOURCE=ROBOTBUILDER ID=COMMAND_DECLARATIONS

		// Determine if the Robot should shoot the boulder
		if (ShotChoice == Constants.LOW_GOAL)
		{
			// Drive over the low bar
			addSequential(new ChassisDriveStraightDistance(475, 0.7));
			
			// TODO: Add Autonomous low bar shoot low goal
		}
		else if (ShotChoice == Constants.HIGH_GOAL)
		{
			// Drive over the low bar
			addSequential(new ChassisDriveStraightDistance(475, 0.7));

			// Raise the shooter while turning towards the goal
			addSequential(new ShooterPositionControl(47.0));
			addSequential(new ChassisTurnAngle(45.0, 0.4));

			// Line up based on camera
			addSequential(new ChassisTurnToImageTarget(Constants.TARGET_LEFT));

			// Shoot into the goal
			addSequential(new ShootBoulderInGoal(47.0, 1.0));
			addSequential(new ShooterSetWheelSpeed(0.0));
		}
		else // No shot but drive over low bar
		{
			// Drive over the low bar
			addSequential(new ChassisDriveStraightDistance(475, 0.7));
		}
	}
}
